shamefully-hoist=true

# Windows + Expo Android native build: pnpm symlink layout makes CMake/ninja
# loop forever ("manifest build.ninja still dirty after 100 tries"). Hoisted
# layout is Expo's recommended fix. See: docs.expo.dev/guides/monorepos
node-linker=hoisted
